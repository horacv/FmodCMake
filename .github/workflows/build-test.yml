name: Build and Test

on:
  push:
    branches: [ master, CICD_feature ]
  pull_request:
    branches: [ master ]

jobs:
  build-and-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        build_type: [Debug, Release]
        include:
          - os: windows-latest
            platform: windows
            api_version: "20312"
          - os: ubuntu-latest
            platform: linux
            api_version: "20312"
          - os: macos-latest
            platform: mac
            api_version: "20312"

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Install 7-Zip (Windows)
        if: runner.os == 'Windows'
        run: choco install 7zip -y

      - name: Setup FMOD API
        env:
          FMOD_USERNAME: ${{ secrets.FMOD_USERNAME }}
          FMOD_PASSWORD: ${{ secrets.FMOD_PASSWORD }}
          PYTHONIOENCODING: utf-8
        run: |
          python tools/setup_fmod_api.py "${{ env.FMOD_USERNAME }}" "${{ env.FMOD_PASSWORD }}" ${{ matrix.platform }} ${{ matrix.api_version }} --delete-installer

      - name: Configure CMake
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}

      - name: Build
        run: |
          cmake --build build --config ${{ matrix.build_type }}

      - name: Run application (Windows)
        if: runner.os == 'Windows'
        timeout-minutes: 1
        run: |
          cd build
          Start-Process -FilePath ".\${{ matrix.build_type }}\FmodCmake.exe" -PassThru | ForEach-Object {
            Start-Sleep -Seconds 10
            if (!$_.HasExited) { Stop-Process -Id $_.Id -Force }
          }

      - name: Run application (Linux/macOS)
        if: runner.os != 'Windows'
        timeout-minutes: 1
        run: |
          cd build
          timeout 10s ./FmodCmake || test $? -eq 124

      - name: Upload build artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ matrix.os }}-${{ matrix.build_type }}
          path: |
            build/
            !build/**/*.o
            !build/**/*.obj
